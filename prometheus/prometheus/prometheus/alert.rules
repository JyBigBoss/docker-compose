groups:
- name: node
  rules:
  - alert: server_status
    expr: up{job="node-exporter"} == 0
    for: 1s
    labels: 
      severity: "严重"
    annotations:
      summary: "node-exporter {{ $labels.instance }} 凉了"
      description: "{{$labels.instance}}  Node-exporter 状态为：down"

